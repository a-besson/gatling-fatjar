<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logback config used only by the e2e run (passed with
  -Dlogback.configurationFile). The one packaged in the fat jar keeps the root
  logger at WARN, which hides the CLI's own INFO lines. The e2e asserts on
  those lines, so it raises the application logger to INFO while leaving
  Gatling/Netty at WARN to keep the CI log readable.
-->
<configuration>

  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%-5level] %logger{15} - %msg%n%rEx</pattern>
    </encoder>
    <immediateFlush>true</immediateFlush>
  </appender>

  <logger name="com.gatling.lab" level="INFO" />

  <root level="WARN">
    <appender-ref ref="CONSOLE" />
  </root>

</configuration>
